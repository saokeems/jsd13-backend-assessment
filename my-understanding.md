# My Understanding

ตอบแต่ละคำถามด้วยคำพูดของคุณเอง ไม่มีคำถามหลอกล่อ

เป้าหมายไม่ใช่คำตอบที่สมบูรณ์แบบ แต่เป็นคำตอบที่ตรงไปตรงมา เขียนเหมือนกำลังอธิบายให้เพื่อนที่ไม่เคยใช้ Express หรือ React ฟัง Assessment นี้ไม่มีวิดีโอ ดังนั้นเอกสารนี้คือจุดที่ความเข้าใจของคุณจะถูกประเมินจริง ๆ — ให้ทำอย่างตั้งใจ

อย่า copy จาก documentation, comment ในโค้ดของคุณ, หรือ AI output ถ้าไม่แน่ใจเรื่องไหน ให้เขียนเท่าที่คุณเข้าใจ และระบุว่าส่วนไหนยังไม่แน่ใจ

---

## AI Code Contribution

rate ตัวเองอย่างตรงไปตรงมาโดยใช้ scale ด้านล่างนี้ rating นี้ไม่ถูกให้คะแนนด้วยตัวมันเอง — ไม่มีตัวเลขไหนที่ "ดีที่สุด" สิ่งที่สำคัญคือมันตรงไปตรงมาและตรงกับสิ่งที่โค้ดและคำตอบของคุณแสดงให้เห็นจริง

| Rating | คำอธิบาย                                                                                                                                                      |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0      | **ไม่ได้ใช้ AI เลย** ฉันไม่ได้ใช้ AI สร้างโค้ด อธิบาย concept debug หรือสอนฉันเลย                                                                             |
| 1      | **ใช้ AI เพื่อเรียนรู้เท่านั้น** ฉันไม่ได้ใช้ AI สร้างโค้ด แต่ใช้ AI ช่วยอธิบาย concept ไขข้อสงสัยเรื่อง error หรือช่วยให้เข้าใจมากขึ้น                       |
| 2      | **เขียนโค้ดเองผสมกับใช้ AI ช่วย** ฉันเขียนโค้ดเองบางส่วน และใช้โค้ดที่ AI สร้างบางส่วน รวมถึงใช้ AI ช่วยให้เข้าใจ debug หรือปรับปรุง solution ของฉัน          |
| 3      | **เรียนรู้จากโค้ดที่ AI สร้าง แล้วเขียนเอง** AI สร้างโค้ดตัวอย่างหรือให้คำแนะนำ แต่ฉันใช้ความเข้าใจนั้นมาเขียนหรือปรับโค้ดสุดท้ายด้วยตัวเอง                   |
| 4      | **AI สร้างโค้ดให้ แต่ฉันเข้าใจมันอย่างครบถ้วน** AI สร้างโค้ดส่วนใหญ่หรือทั้งหมด แต่ฉันอธิบายได้ว่ามันทำงานอย่างไร ทำไมถึงทำงาน และส่วนหลัก ๆ เชื่อมกันอย่างไร |
| 5      | **AI สร้างโค้ดให้ แต่เข้าใจอย่างจำกัด** AI สร้างโค้ดส่วนใหญ่หรือทั้งหมด และฉันไม่สามารถอธิบายได้อย่างมั่นใจว่าทุกอย่างทำงานอย่างไรหรือทำไมถึงทำงาน            |

**Rating ของฉัน:** \_\_\_

> ถ้า rate ไว้ **2 ขึ้นไป** ให้ตอบส่วน "AI Process" ที่ท้ายเอกสารนี้ด้วย

---

## Backend

**1. HTTP method แต่ละตัวในแอปของคุณหมายถึงอะไร — GET, POST, PUT or PATCH, และ DELETE? ทำไมเราถึงใช้ method ต่างกัน แทนที่จะใช้ POST สำหรับทุกอย่าง?**

_คำตอบของคุณ:_

- GET ตัวแรกใช้ดึงข้อมูลสินค้าทั้งหมดและใช้ค้นหาข้อมูลตาม query string
- GET ตัวที่สองใช้ดึงข้อมูลสินค้าเฉพาะ ID ที่เลือก
- POST ใช้ส่งข้อมูลเพื่อสร้างสินค้าใหม่
- PUT/PATCH ใช้แก้ไขข้อมูลสินค้าที่มีอยู่ตาม ID ที่เลือก
- DELETE ใช้ลบข้อมูลสินค้าตาม ID ที่เลือก
- แทนที่จะใช้ POST อย่างเดียว การใช้ตัวอื่นๆจะช่วยให้ API ของเรามีความเป็นสากล ช่วยให้โค้ดอ่านเข้าใจง่าย ปลอดภัย และมีความชัดเจนตรงตาม METHOD

---

**2. `express.json()` คืออะไร และจะเกิดอะไรขึ้นถ้าคุณไม่ใส่มัน?**

_คำตอบของคุณ:_

- คือ built-in Middleware ของตัว Express มีหน้าที่คอยแปลงข้อมูลแบบ JSON ที่ส่งมากับ Body ให้เป็น JS Object แบบ auto และถ้าไม่ใส่ built-in ตัวนี้มาจะทำให้เวลาส่งข้อมูลเข้ามา ตัว server จะอ่านค่าไม่ออก ทำให้ค่าออกมาเป็น undefined และแก้หรือบันทึกไม่ได้

---

**3. `req.body`, `req.params`, และ `req.query` ต่างกันอย่างไร? ยกตัวอย่างจริงจาก API ของคุณสำหรับแต่ละตัว**

_คำตอบของคุณ:_

- req.body คือข้อมูลที่ส่งมากับ Payload ของ request ใช้กับ POST PUT เช่นตอนสร้าง products ขึ้นมาใหม่

  {
  name:"Noodle Cup",
  price:"20",
  quantity:"30"
  }

- req.params คือตัวแปรที่อยู่ใน URL Path ใช้ระบุตัวเจาะจง เช่น เวลาที่เราจะแก้หรือลบสินค้า ทำได้ผ่าน path /products/:id ก็จะได้ค่าเป็น req.params.id เป็นรหัสสินค้านั้นๆ

- req.query เป็น พารามิเตอร์ที่ต่อท้าย URL ถัดจากเครื่องหมาย (?) ใช้ทำ filter หรือ query เช่น /products?name=Monitor และจะได้จากค่า req.query.name

---

**4. HTTP status codes คืออะไร? ระบุรายการ status code ทุกตัวที่คุณใช้ใน API และอธิบายว่าทำไมถึงเลือกใช้ในแต่ละสถานการณ์**

_คำตอบของคุณ:_

- HTTP status codes คือ รหัส 3 หลักที่ server ส่งกลับมาตอน res เพื่อบอกสถานะว่าสำเร็จหรือผิดพลาด

- 200 OK ใช้ตอนสำเร็จแบบปกติ
- 201 Created Success ใช้ตอนสร้างข้อมูลใหม่สำเร็จ
- 400 Bad Request ใช้ตอนข้อมูลไม่ถูกต้องหรือขาดข้อมูล
- 404 Not Found ใช้ตอนไม่เจอข้อมูลที่เรียก
- 500 Internal Server Error ใช้ตอนที่ server error

---

**5. middleware คืออะไร? อธิบายด้วยคำพูดของคุณเองว่ามันทำอะไร พร้อมยกตัวอย่าง 1 อย่างจากโค้ดของคุณ**

_คำตอบของคุณ:_

- middleware คือตัวกลางที่คอยสื่อสารระหว่างที่คำขอวิ่งเข้า server ก่อนส่งไป route handler ตัวสุดท้าย
- เช่น Express.json() ที่คอยแปลงข้อมูลให้สามารถคุยกับ server ได้

---

**6. ทำไม order ของ middleware ใน Express ถึงสำคัญ? จะเกิดอะไรขึ้นถ้า order ผิด?**

_คำตอบของคุณ:_

- เพราะ Express ทำงานโดยอ่านโค้ดจากบนลงล่างตามลำดับ
- ถ้าลำดับผิดจะทำให้เกิด Error เช่น express.jon() วางผิดที่แทนที่จะแปลงข้อมูลก่อน ก็จะไปทำอย่างอื่นก่อน ทำให้ค่าที่อ่านเป็น undefined แทนได้

---

**7. อธิบายทีละขั้นตอนว่าเกิดอะไรขึ้นบน server เมื่อมี POST request ถูกส่งไปที่ `/products`**

_คำตอบของคุณ:_

- server รับ post request จากเส้น /products
- req วิ่งไป global middleware ต่างๆ เช่น express.json()
- req เข้า route handler ของ post
- โค้ดใน route ทำงาน
- บันทึกข้อมูลลง db หรือ mock db
- server ส่ง res กลับพร้อม status code และส่งข้อมูลที่สร้าง

---

**8. CRUD คืออะไร? จับคู่แต่ละ operation กับ HTTP method และ route ที่คุณใช้ใน API**

_คำตอบของคุณ:_

- C = Create -> Method POST -> /products
- R = Read -> Method GET -> /products | /products/:id
- U = Update -> Method PUT | PATCH -> /products/:id
- D = Delete -> Method DELETE -> /products/:id

---

**9. API ของคุณตอบสนองอย่างไรเมื่อมีอะไรผิดพลาด — เช่น เมื่อ product ตาม ID ที่ระบุไม่มีอยู่จริง?**

_คำตอบของคุณ:_

- server จะส่ง res status code กลับมากับข้อความ error เป็นรูปแบบ JSON

---

## Frontend & Integration

**10. CORS คืออะไร และแก้ปัญหาอะไร? ถ้าไม่ได้ config ไว้บน server ของคุณ คุณจะเห็นอะไรใน browser?**

_คำตอบของคุณ:_

- CORS(Cross-Origin Resource Sharing) คือระบบ safety ของเว็บเบราว์เซอร์ ที่คอยบล็อคไม่ให้คนละ Domain หรือคนละ Port ดึงข้อมูลโดยตรง และให้ Server บางอันได้รับอนุญาตเข้าถึงสิทธิ์ได้

---

**11. แอป React ของคุณ fetch ข้อมูลจาก API ที่ไหน? อธิบายว่า `useEffect` ในโค้ดนั้นทำอะไร และทำไมถึงเรียก fetch ตรง ๆ ใน component body ไม่ได้**

_คำตอบของคุณ:_

- fetch จาก API ผ่าน .env (import.meta.env.VITE_API_URL)
- useEffect ทำหน้าที่จัดการ Side Effects ให้เรียกฟังก์ชัน fetchProducts() แค่ครั้งเดียวตอน component render ครั้งแรก
- หากเรียกตรงๆ จะทำให้เกิด Infinite Loop จนแอป crash ได้

---

**12. API base URL ของคุณถูกกำหนดไว้ที่ไหน และทำไมถึงเลือกเก็บไว้ตรงนั้น แทนที่จะ hardcode ไว้ในทุก fetch call?**

_คำตอบของคุณ:_

- กำหนดไว้ในไฟล์ .env ใน client ด้วยชื่อ VITE_API_URL
- ทำให้ง่ายเวลามาปรับแก้ทีหลัง และแก้จุดเดียว

---

**13. เลือก action หนึ่งในแอปของคุณ — เช่น การลบ product อธิบายการเดินทางแบบครบวงจร (full round trip): เกิดอะไรขึ้นตั้งแต่ผู้ใช้คลิกปุ่ม ไปจนถึง request ไปถึง server จนถึงหน้าจออัปเดตด้วย list ใหม่**

_คำตอบของคุณ:_

- การลบ product
- กดปุ่ม "ลบ"
- ฟังก์ชัน handleDelete(id) ทำงาน
- axios ส่ง DELETE req ไปยัง ${API_URL}/${id}
- server รับ req ตรวจ id และลบสินค้า
- server ส่ง res กลับว่า ลบสำเร็จ
- client รับ res และอัปเดต โดยใช้ ฟังก์ชั่น .filter()
- re-render หน้าจอ แสดง list ใหม่

---

**14. แอปของคุณแสดงอะไรให้ผู้ใช้เห็นระหว่างที่ข้อมูลกำลังโหลด และแสดงอะไรถ้า fetch ล้มเหลว (เช่น server ไม่ได้รันอยู่)? ทำไมเรื่องนี้ถึงสำคัญ?**

_คำตอบของคุณ:_

- ระหว่างโหลด แสดงข้อความ "กำลังโหลดข้อมูล..."
- ถ้า fetch ล้มเหลว แสดงกล่องเตือน error สีแดงว่า "Error fetching products"
- ช่วยในเรื่อง UX ดีขึ้น จะรู้ว่าระบบทำอะไรอยู่

---

**15. หลังจากที่คุณ add, edit, หรือ delete product แล้ว list บนหน้าจอของคุณอัปเดตโดยไม่ต้อง refresh หน้าเว็บ อธิบายว่าทำไมถึงเป็นแบบนั้น — อะไรที่ทำให้ React re-render ด้วยข้อมูลใหม่?**

_คำตอบของคุณ:_

- เพราะเราใช้ตัว React State
- เมื่อมี Action ต่างๆ จะทำการเรียก fetchProducts() ซ้ำเพื่อเรียกข้อมูลใหม่มาทับ
- การเปลี่ยนค่าใน State สั่งให้ React re-render ใหม่ auto เพื่ออัพเดทข้อมูลล่าสุดโดยไม่ต้องรีเฟรชหน้าเบราว์เซอร์ใหม่

---

**16. ส่วนไหนที่ยากที่สุดในการเชื่อมแอป React ของคุณเข้ากับ Express API และคุณทำอย่างไรถึงผ่านมันมาได้?**

_คำตอบของคุณ:_

- ไม่มีอะไรยากเกินไปครับ แค่ไปอ่าน docs วิธีการใช้งาน CORS ก็สามารถเข้าใจหลักการทำงานของมันได้ในระดับนึงแล้วครับ

---

## AI Process

ตอบส่วนนี้เฉพาะถ้าคุณ rate ตัวเอง **2 ขึ้นไป** บน AI Code Contribution Scale ด้านบน ถ้า rate ไว้ 0 หรือ 1 ให้เขียน "N/A" ใต้แต่ละคำถาม

**17. ถ้าคุณใช้ AI สร้างโค้ด คุณแบ่งงานออกเป็นขั้นตอนหรือ prompt อย่างไร? ยกตัวอย่าง prompt จริงที่คุณใช้ 1 อัน แทนที่จะเป็น prompt เดียวแบบ "สร้างทั้งแอปให้หน่อย"**

_คำตอบของคุณ:_

- ใช้ AI ในการวิธีการดึง API_URL ของ react และการตั้งชื่อในไฟล์ .env ของ react เพราะตอนแรกตั้งชื่อเลย prompt ถามว่า "ทำไมพอใส่ตรงๆใน get มันได้ พอตั้งใน .env ดันไม่ได้" สรุปอยู่ที่การตั้งชื่อใน .env ของ react

---

**18. อธิบายสิ่งที่ AI tool สร้างให้ 1 อย่างที่คุณเปลี่ยน แก้ไข หรือปฏิเสธ — พร้อมเหตุผลว่าทำไม**

_คำตอบของคุณ:_

- ให้ AI ช่วยแบ่ง component react ให้ เพราะรู้สึกว่า เขียนใน App.jsx อันเดียวมันเยอะเกินไป

---

**19. อธิบาย bug หรือ error จริง ๆ ที่คุณเจอระหว่าง build โปรเจกต์นี้ 1 อย่าง คุณหาสาเหตุที่แท้จริงได้อย่างไร นอกเหนือจากการ copy error ไปถามใน chat?**

_คำตอบของคุณ:_

- ระหว่าง build โปรเจกต์ ตอนทำในส่วนของ server ในขณะที่ลองรัน Method POST แล้วข้อมูลไม่มาตาม ไม่ success ก็อ่านจาก error ที่มันฟ้องว่า not fount เลยไล่หาตั้งแต่ route ที่เขียนและ code ที่เขียนภายในว่าพลาดตรงจุดไหนจนเจอ bug โดยไม่ได้ถาม chat

---

**20. เลือก route (backend) หรือ component (frontend) 1 อันที่ AI ช่วยสร้าง โดยไม่ย้อนกลับไปดู AI chat history อธิบายว่ามันทำอะไรและทำไมถึงทำงาน ด้วยคำพูดของคุณเอง**

_คำตอบของคุณ:_

- component ProductForm.jsx, ProductTable.jsx ที่ให้ AI ช่วยแยกให้ AI ช่วยแยกระหว่าง Form ที่ใช้กรอกข้อมูล เพิ่มหรืออัพเดทข้อมูลที่กรอกไป ส่วน Table ก็จะเป็นในส่วนของตารางที่ใช้แสดงข้อมูลของสินค้าที่ดึงมา
